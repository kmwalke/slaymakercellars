<% if town.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(town.errors.count, "error") %> prohibited this town from being saved:</h2>

    <ul>
      <% town.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= form.label :name, autofocus: 'autofocus' %>
  <%= form.text_field :name %>
</div>

<div class="field">
  <%= form.label :state %>
  <%= form.collection_select :state_id, State.order(:name), :id, :name, include_blank: true %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
