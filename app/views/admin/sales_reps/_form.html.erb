<% if sales_rep.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(sales_rep.errors.count, "error") %> prohibited this sales_rep from being saved:</h2>

    <ul>
      <% sales_rep.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= form.label :name, autofocus: 'autofocus' %>
  <%= form.text_field :name %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
