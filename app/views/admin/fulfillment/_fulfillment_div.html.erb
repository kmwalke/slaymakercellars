<div class="day">

  <h2><%= title %></h2>

  <table class="crud fulfillment">
    <tr class="title">
      <th>Type</th>
      <th>Cases</th>
      <th>Bottles</th>
    </tr>

    <% @fulfillment.product_names.each do |product| -%>
      <% amount = date.nil? ? @fulfillment.total_amount(product) : @fulfillment.amount(product, date) -%>
      <tr>
        <td><%= product %></td>
        <td class=<%= amount[:cases] > 0 ? 'active' : 'inactive' %>>
          <%= amount[:cases] %>
        </td>
        <td class=<%= amount[:bottles] > 0 ? 'active' : 'inactive' %>>
          <%= amount[:bottles] %>
        </td>
      </tr>
    <% end -%>

  </table>
</div>
