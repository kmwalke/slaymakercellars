<h1>Editing <%= @state.name %></h1>

<%= form_for(@state, url: admin_state_path(@state), html: { multipart: true, autocomplete: 'off' }) do |form| %>
  <%= render 'form', state: @state, form: form %>
<% end -%>

<%= link_to 'Back', admin_states_path %>
