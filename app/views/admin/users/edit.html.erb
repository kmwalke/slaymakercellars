<h1>Editing <%= @user.name %></h1>

<%= form_for(@user, url: admin_user_path(@user), html: { multipart: true, autocomplete: 'off' }) do |form| %>
  <%= render 'form', user: @user, form: form %>
<% end -%>

<%= link_to 'Back', admin_users_path %>
